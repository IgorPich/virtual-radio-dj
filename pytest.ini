[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --strict-markers
    --tb=short
markers =
    manual: marks tests requiring real external services (Spotify, Ollama, audio)
    unit: fast isolated unit tests
    integration: tests that compose multiple real modules together
